name: Keep Render Warm Backup

on:
  schedule:
    - cron: "2,12,22,32,42,52 * * * *"  # every 10 minutes offset by 2
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend (Backup)
        run: |
          echo "🔄 Backup health check at $(date)"
          curl -s https://subliminalgenbackend.onrender.com/health || true